import { EmptyState } from '@studdy/design-system';

export default function Page() {
  return (
    <EmptyState
      title="Organisation workspace"
      description="Organisation tools arrive in a later release."
    />
  );
}
